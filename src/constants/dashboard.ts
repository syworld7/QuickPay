/** Default rows per page for the transaction history table */
export const DEFAULT_TRANSACTIONS_PAGE_SIZE = 10;

/** Page size options for the transaction table dropdown */
export const TRANSACTIONS_PAGE_SIZE_OPTIONS = [5, 10, 20, 50] as const;
/** Max rows fetched once for charts and summary metrics */
export const TRANSACTIONS_CHART_LIMIT = 500;

